# VERIFY BEFORE FIRST RUN — WAN BOOTH Phase 2.6

Generated by forge-polecat-B::wan-booth-phase26::S259

These fields were written to `app/workflows/i2v_14B_2stage.json` based on the
Phase 2.6 brief. The custom node source files (KJNodes, ComfyUI-TeaCache) do NOT
exist on this Mac — they live on the Windows RTX PC. Before the first inference
run, verify each item below against the installed source on the Windows PC.

---

## PatchSageAttentionKJ (nodes 20, 22)

**Field:** `sageattn_type`
**Value used:** `"sageattn_qk_int8_pv_fp16_cuda"`
**Verify against:** `ComfyUI-KJNodes/nodes.py` — find `PatchSageAttentionKJ` in
`NODE_CLASS_MAPPINGS` and check the allowed values for `sageattn_type`. The string
above must be a valid enum member. Common alternatives include
`sageattn_qk_int8_pv_fp8_cuda` or `sageattn_qk_int8_pv_fp16_hip` — confirm which
variants your build of sageattention supports.

---

## ApplyTeaCache (nodes 21, 23)

**Field:** `retention_mode`
**Value used:** `true` (boolean)
**Verify against:** `ComfyUI-TeaCache/nodes.py` — confirm the field is named
`retention_mode` (not `use_retention_mode`, `ret_mode`, etc.) in your installed
version. Some releases renamed this field.

---

**Field:** `cache_device`
**Value used:** `"cuda"`
**Verify against:** `ComfyUI-TeaCache/nodes.py` — confirm allowed values. Expected
options are `"cuda"`, `"cpu"`, or `"auto"`. If the installed version does not accept
`"cuda"` directly (e.g., requires `"gpu"`), update nodes 21 and 23.

---

**Field:** `model_type`
**Value used:** `"wan2.1_i2v_480p_14B_ret_mode"`
**Verify against:** `ComfyUI-TeaCache/nodes.py` — confirm this exact string is a
valid enum member for `model_type`. The brief targets the 14B I2V 480p model with
retention mode enabled. If your TeaCache build uses a different enum (e.g.,
`"wan2.1_i2v_14B"` without the resolution/ret_mode suffix), update nodes 21 and 23.

---

## Node Wiring Reference (post-surgery)

| Node | class_type | model input |
|------|-----------|-------------|
| 20 | PatchSageAttentionKJ (Stage 1) | ["18", 0] — last Stage 1 LoRA |
| 21 | ApplyTeaCache (Stage 1) | ["11", 0] — Stage 1 ModelSamplingSD3 |
| 22 | PatchSageAttentionKJ (Stage 2) | ["19", 0] — last Stage 2 LoRA |
| 23 | ApplyTeaCache (Stage 2) | ["12", 0] — Stage 2 ModelSamplingSD3 |
| 11 | ModelSamplingSD3 (Stage 1) | ["20", 0] — rewired to SA node |
| 12 | ModelSamplingSD3 (Stage 2) | ["22", 0] — rewired to SA node |
| 13 | KSamplerAdvanced (Stage 1) | ["21", 0] — rewired to TeaCache node |
| 14 | KSamplerAdvanced (Stage 2) | ["23", 0] — rewired to TeaCache node |

---

*If any field name or enum value does not match, update the workflow JSON before
running inference. A mismatched field will cause a ComfyUI node load error.*
